
import { Meta } from '@storybook/addon-docs/blocks';
import { AmazonButton } from '../src/Elements/AmazonButton';

<Meta title="Buttons/AmazonButton" component={AmazonButton} />

# AmazonButton

The `AmazonButton` component is used for rendering a customizable button with Amazon-like styling.

## Default

This section demonstrates the default rendering of the `AmazonButton` component.

```jsx
import React from 'react';
import AmazonButton from 'react-craftify-buttons';

const MyComponent = () => {
  return (
    <div>
      {/* Default usage */}
      <AmazonButton>
       AmazonButton
      </AmazonButton>
    </div>
  );
};
```

## Customized

This section demonstrates a customized rendering of the AmazonButton component.

You can customize the appearance of the button using props.

```jsx
import React from 'react';
import AmazonButton from 'react-craftify-buttons';

const MyComponent = () => {
  return (
   <AmazonButton
    backgroundColor="#ff9900"
    borderColor="#ff9900"
    textColor="#ffffff"
    fontSize={16}
    width="150px"
   >
     Custom Button
   </AmazonButton>
  );
};
 
```