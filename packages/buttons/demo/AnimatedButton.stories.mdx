import { Meta, Story } from '@storybook/addon-docs/blocks';
import AnimatedButton from '../src/Elements/AnimatedButton.tsx';

<Meta title="Buttons/AnimatedButton" component={AnimatedButton} />

# AnimatedButton

A button with animated hover and active effects.

## Default

```tsx
import React from 'react';
import AnimatedButton from '../src/Elements/AnimatedButton.tsx';

export const Template: Story = (args) => <AnimatedButton {...args}>Hover Me!</AnimatedButton>;

export const Default = Template.bind({});
Default.args = {
  children: 'Hover Me!',
};
