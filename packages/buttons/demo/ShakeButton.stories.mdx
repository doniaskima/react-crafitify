import { Meta, Story } from '@storybook/addon-docs/blocks';
import ShakeButton from '../src/Elements/StyledButton.tsx';

<Meta title="Buttons/ShakeButton" component={ShakeButton} />


# ShakeButton

A button component with a shaking animation effect on hover.

## Usage

The `ShakeButton` component provides a visual effect where the button shakes slightly when hovered over. To use it:


```jsx
<ShakeButton>Click Me!</ShakeButton>