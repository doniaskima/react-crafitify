import { Meta } from '@storybook/addon-docs/blocks';
import BlobButton from '../src/Elements/BlobButton';

<Meta title="Buttons/BlobButton" component={BlobButton} />

# BlobButton

```jsx
import React from 'react';
import BlobButton from  'react-craft-buttons';  

const MyComponent = () => {
  return (
    <div>
      {/* Default usage */}
      <BlobButton>Conic Button</BlobButton>
    </div>
  );
};
```

## Customized Blob Button

```jsx
import React from 'react';
import BlobButton from  'react-craft-buttons';  

const MyComponent = () => {
  return (
    <div>
    <BlobButton
    padding="20px 40px"
    fontSize="20px"
    textColor="red"
    hoverTextColor="white"
    borderColor="green"
    borderSize="3px"
    borderRadius="50px"
    innerBackgroundColor="blue"
    blobColor="yellow"
    blobWidth="30%"
  >
    Customized Blob Button
   </BlobButton>
    </div>
  );
};
```

 