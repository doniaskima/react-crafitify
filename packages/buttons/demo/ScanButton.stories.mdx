import { Meta, Story } from '@storybook/addon-docs/blocks';
import ScanButton from '../src/Elements/ScanButton.tsx';

<Meta title="Buttons/ScanButton" component={ScanButton} />


# ScanButton

A button component with a unique scanning animation effect.

## Usage

The `ScanButton` component offers an engaging button with a scanning animation effect. It changes its appearance when hovered and clicked. To use it:

```jsx
<ScanButton>Click Me!</ScanButton>

 