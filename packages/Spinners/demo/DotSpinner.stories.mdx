import { Meta, Story } from '@storybook/addon-docs/blocks';
import DotSpinner, { DotSpinnerProps } from  "../src/Elements/DotSpinner"

<Meta title="Spinners/DotSpinner" component={DotSpinner}  />

# 🔵 DotSpinner: Spinning Dots for a Dynamic UI! 🌀

The `DotSpinner` component displays a mesmerizing set of spinning dots in a circular pattern, adding dynamism and elegance to your UI. ✨🔵

## Usage 🛠️

```jsx
import React from 'react';
import {DotSpinner} from 'react-craftify-spinners';

const MyComponent = () => {
  return (
    <div>
      <h1>Loading</h1>
      <DotSpinner />
    </div>
  );
};
