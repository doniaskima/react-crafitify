import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import PlaceholderLoader, { PlaceholderLoaderProps } from '../src/Elements/PlaceholderLoader';

<Meta
  title="Spinners/PlaceholderLoader"
  component={PlaceholderLoader}
/>

# PlaceholderLoader

The PlaceholderLoader component displays a loading animation with placeholder elements.

## Default

```jsx
import React from 'react';
import NewtonsCradle from 'react-craftify-spinners';

const MyComponent = () => {
  return (
    <div>
      <h1>Loading</h1>
        <PlaceholderLoader width="250px" height="130px" />
    </div>
  );
};
```
