import { Meta } from '@storybook/addon-docs/blocks';
import { WobblingLoader } from '../src/Elements/Wobbling';

<Meta title="Spinners/WobblingLoader" component={WobblingLoader} />

# WobblingLoader

Introducing the **WobblingLoader** component, your tool to infuse anticipation and excitement into your UI.  

Whether you're building a search-centric app or just want to add a touch of exploration to your design, the WobblingLoader is here to turn your UI into a realm of possibilities.

## How to Use:

```jsx
import React from 'react';
import { WobblingLoader } from 'react-craftify-spinners';

function App() {
  return (
    <div>
      <WobblingLoader size="20px" /> {/* Customize the size as needed */}
    </div>
  );
}
