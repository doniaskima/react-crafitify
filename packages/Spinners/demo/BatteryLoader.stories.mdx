import { Meta } from '@storybook/addon-docs/blocks';
import { BatteryLoader } from '../src/Elements/BatteryLoader';

<Meta title="Spinners/BatteryLoader" component={BatteryLoader} />

# BatteryLoader

Introducing the **BatteryLoader** component, your tool to infuse anticipation and excitement into your UI.  

Whether you're building a search-centric app or just want to add a touch of exploration to your design, the BatteryLoader is here to turn your UI into a realm of possibilities.

## How to Use:

```jsx
import React from 'react';
import { BatteryLoader } from 'react-craftify-spinners';

function App() {
  return (
    <div>
      <BatteryLoader size="20px" color="#000" /> {/* Customize the size and color as needed */}
    </div>
  );
}
```