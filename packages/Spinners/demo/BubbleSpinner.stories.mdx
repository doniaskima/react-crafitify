import { Meta } from '@storybook/addon-docs/blocks';
import { BubbleSpinner } from '../src/Elements/BubbleSpinner/BubbleSpinner';

<Meta title="Spinners/BubbleSpinner" component={BubbleSpinner} />

# 🎈 BubbleSpinner: Playful Animation for Your Delightful UI!

Introducing the *BubbleSpinner*, a tiny ball of joy ready to spin and dance its way into your heart. This enchanting component brings life to your user interface with its captivating animations. 🎈🌀

With a touch of magic, the BubbleSpinner creates a mesmerizing whirl of bubbles that's impossible to resist. It's like having a cheerful friend in your app, always ready to spread smiles and sprinkle a dash of playfulness wherever it appears.

## Features ✨:

- **Auto Play**: BubbleSpinner comes alive as soon as it's summoned, so your users are greeted with a delightful surprise every time.
- **Endless Dance**: The fun never stops! BubbleSpinner gracefully spins and loops endlessly, creating a captivating visual treat.
- **Easy Integration**: With a simple yet powerful API, you can effortlessly drop the BubbleSpinner into your UI and watch the magic unfold.

Bring joy and animation to your project with the BubbleSpinner. Let it whirl, twirl, and spin its way into your users' hearts, leaving a trail of happiness in its wake. 🎉✨

## Default Usage

```jsx
import React from 'react';
import {BubbleSpinner} from 'react-craftify-spinners';  

const MyComponent = () => {
  return (
    <div>
      <BubbleSpinner style={{ width: '200px', height: '200px' }} />
    </div>
  );
};

```

## Custom Style 🎨

```jsx
import React from 'react';
import {BubbleSpinner} from 'react-craftify-buttons';  

const MyComponent = () => {
  return (
    <div>
     <BubbleSpinner style={{ width: '300px', height: '300px', backgroundColor: 'lightgray' }} />
    </div>
  );
};

```

