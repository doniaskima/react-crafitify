import { Meta } from '@storybook/addon-docs/blocks';
import { EyesLoader } from '../src/Elements/EyesLoader';

<Meta title="Spinners/EyesLoader" component={EyesLoader} />

# EyesLoader

Introducing the **EyesLoader** component, your tool to infuse anticipation and excitement into your UI.  

Whether you're building a search-centric app or just want to add a touch of exploration to your design, the EyesLoader is here to turn your UI into a realm of possibilities.

## How to Use:

```jsx
import React from 'react';
import { EyesLoader } from 'react-craftify-spinners';

function App() {
  return (
    <div>
      <EyesLoader size="20px" /> {/* Customize the size as needed */}
    </div>
  );
}
