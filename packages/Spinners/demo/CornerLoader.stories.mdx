import { Meta, Story } from '@storybook/addon-docs/blocks';
import { CornerLoader } from '../src/Elements/CornerLoader';  

<Meta title="Spinners/CornerLoader" component={CornerLoader} />

# 🔄 CornerLoader: Loading Fun for Your UI! 🔄

Introducing the *CornerLoader* component, a nifty way to add some loading fun to your user interface. Get ready for a whirlwind of excitement as this loader spins its way into your design. 🌪️

Whether you're creating a content-rich app or just want to add a playful twist to your loading screens, the CornerLoader is here to make your UI lively and engaging.

## Default Usage 🔃

```jsx
import React from 'react';
import CornerLoader from 'react-craftify-spinners';

const MyComponent = () => {
  return (
    <div>
      <h1>Loading</h1>
      <CornerLoader />
    </div>
  );
};
```

## Custom Color 🎨

You can customize the color of the loader by using the color prop. 🌈

```jsx
import React from 'react';
import CornerLoader from 'react-craftify-spinners';

const MyComponent = () => {
  return (
    <div>
      <h1>Loading</h1>
      <CornerLoader color="#ff0000" />
    </div>
  );
};
;
```

## With Custom Class 🚀

You can customize the loader's appearance by adding your custom class. 🆒

```jsx
import React from 'react';
import CornerLoader from 'react-craftify-spinners';

const MyComponent = () => {
  return (
    <div>
      <h1>Loading</h1>
      <CornerLoader className="custom-class" />
    </div>
  );
};
;
```