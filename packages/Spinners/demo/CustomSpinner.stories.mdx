import { Meta, Story } from '@storybook/addon-docs/blocks';
import CustomSpinner from '../src/Elements/CustomSpinner';

<Meta title="Spinners/CustomSpinner" component={CustomSpinner} />

# 🌀 CustomSpinner: Spin Your Way to Greatness! 🌀

The `CustomSpinner` component is your go-to solution for adding delightful loading spinners to your user interface. Whether you need a default spinner or want to add some color or center it, the `CustomSpinner` has got you covered! 🚀🎨

## Default Spinner Usage 🔃

Use the default `CustomSpinner` as a quick and easy way to indicate loading.

```jsx
import React from 'react';
import CustomSpinner from 'react-craftify-spinners';

const MyComponent = () => {
  return (
    <div>
      <h1>Loading</h1>
      <CustomSpinner />
    </div>
  );
};

```

## Colored Spinner 🌈

```jsx
import React from 'react';
import CustomSpinner from 'react-craftify-spinners';

const MyComponent = () => {
  return (
    <div>
      <h1>Loading</h1>
      <CustomSpinner color="#ff0000" />
    </div>
  );
};
```

## Centered Spinner 🎯

```jsx
import React from 'react';
import CustomSpinner from 'react-craftify-spinners';

const MyComponent = () => {
  return (
    <div>
      <h1>Loading</h1>
      <CustomSpinner center />
    </div>
  );
};

```
