import { Meta } from '@storybook/addon-docs/blocks';
import { SettingLoader } from '../src/Elements/Wobbling';

<Meta title="Spinners/SettingLoader" component={SettingLoader} />

# SettingLoader

Introducing the **SettingLoader** component, your tool to infuse anticipation and excitement into your UI.  

Whether you're building a search-centric app or just want to add a touch of exploration to your design, the SettingLoader is here to turn your UI into a realm of possibilities.

## How to Use:

```jsx
import React from 'react';
import { SettingLoader } from 'react-craftify-spinners';

function App() {
  return (
    <div>
      <SettingLoader size="20px" color="#854f1d" /> {/* Customize the size and color as needed */}
    </div>
  );
}

```