import React from 'react';
import { BaseProps } from './type';
export interface BlocksProps extends BaseProps {
}
export default function BlocksLoader({ visible, width, height, className, wrapperStyle, ariaLabel, }: BlocksProps): React.JSX.Element | null;
